# Practice 6 – Go Concurrency

## Problems

**Problem 1** – Thread-safe maps using `sync.Map` and `sync.RWMutex`  
**Problem 2** – Concurrent counter fixed with `sync.Mutex` and `sync/atomic`  
**Problem 3** – Fan-In pattern merging multiple channels with context cancellation

## Run

```bash
go run -race ./problem1/
go run -race ./problem2/
go run -race ./problem3/
```
